import React, {  useEffect } from 'react'

// useEffect / useLayoutEffect hooks are used for performing side effects 
// and return an optional clean up function.
//  As they do not deal with returning values, no types are necessary.

function useEffectReducer() {
	
	// useEffect(() => {
    //     const subscriber = subscribe(options);
    //     return () => {
    //       unsubscribe(subscriber)
    //     };
    //   }, [options]);
}
export default useEffectReducer